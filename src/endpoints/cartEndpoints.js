// import Cart from "../models/Cart.js";
// import express from "express";
// import { createModelHandler, deleteModelHandler, readModelHandler, updateModelHandler } from "../controllers/crud.js";

// const cartRouter = express.Router();

// cartRouter.post('/create', createModelHandler(Cart));
// cartRouter.get('/get', readModelHandler(Cart));
// cartRouter.get('/get/:id', readModelHandler(Cart));
// cartRouter.put('/edit/:id', updateModelHandler(Cart));
// cartRouter.delete('/delete/:id', deleteModelHandler(Cart));

// export default cartRouter;